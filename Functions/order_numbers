'''This function compares two numbers and returns them in increasing order.'''

def order_numbers(num1,num2):
    if num2 > num1:
        return num1,num2
    return num2,num1

smaller,bigger = order_numbers(100,99)
print('smaller = '+str(smaller),'bigger = '+str(bigger))

#Output
# smaller = 99 bigger = 100