# Swapping values
# swapping values of two variables without re assigning new values to the variable.

a = 20
b = 10

# Method 1

temp = a
a = b 
b = temp
print("a= " + str(a))
print("b= " + str(b))

#Method 2 without using third variable

a = a + b
b = a - b
a = a - b


print("a= " + str(a))
print("b= " + str(b))

# Method 3 

a,b = b,a
print("a= " + str(a))
print("b= " + str(b))

# Method 4

a = a*b
b = a//b
a = a//b
print("a= " + str(a))
print("b= " + str(b))